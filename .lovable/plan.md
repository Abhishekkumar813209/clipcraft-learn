## Change
In `src/pages/SscLayout.tsx` `NAV_ITEMS`:
- Remove the `BB Practice` entry.
- Rename `Black Book Hub` label to `BB Practice` (path stays `/ssc/blackbook`).
- Keep `BB Browse` as-is.

No other files affected.
