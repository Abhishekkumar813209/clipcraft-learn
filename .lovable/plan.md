

## Plan: Change Default Planned Hours to 6

### What
Change the default `plannedHours` state from `8` to `6` in `src/pages/ProductivityCoach.tsx`.

### Change
- **Line 109**: `useState(8)` → `useState(6)`

### Files Modified
- `src/pages/ProductivityCoach.tsx` — One-line default value change

